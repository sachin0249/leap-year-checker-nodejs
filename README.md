# leap-year-checker-nodejs
Node.js program to determine if a given year is a leap year. The program takes user input for a year and uses standard leap year rules (divisible by 4, not 100 unless also 400) to check and display the result.
